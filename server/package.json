{
  "name": "super-dooltocht-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file=.env --watch index.js",
    "cleanup:dry": "node --env-file=.env cleanup.js",
    "cleanup": "node --env-file=.env cleanup.js --confirm"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.1.6"
  }
}
